# Base URL for the BudgetPlate API. Leave blank in local dev to use the Vite
# proxy (/api -> http://localhost:8000). In production (Vercel), set this to
# your deployed Render backend URL, e.g. https://budgetplate-api.onrender.com
VITE_API_BASE=
